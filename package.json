{
  "name": "sapar",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently -n front,back -c cyan,magenta \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\"",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "build": "npm run build --prefix frontend",
    "seed": "npm run seed --prefix backend"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Dirolka/sapar.git"
  },
  "private": true,
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "@clerk/clerk-js": "^6.11.0",
    "@clerk/react": "^6.6.3",
    "jsonwebtoken": "^9.0.3",
    "lucide-react": "^1.16.0",
    "mongoose": "^9.6.2",
    "vite": "^8.0.13"
  }
}
